
export default class HookUp {

    isFalse() {
        return false;
    }

    listMultiples(n) {
        let multiples = [];
        if (n > 3) {
            multiples.push(3);
        }

        return multiples;
    }
}

